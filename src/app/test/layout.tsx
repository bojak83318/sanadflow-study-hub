/**
 * Test Pages Layout
 * Common layout for all test sandbox pages
 * 
 * @module src/app/test/layout
 * @agent qa-engineer
 */

export default function TestLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return children;
}
